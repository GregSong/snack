#include "log_impl.h"

namespace snack
{
    void Register(Logger& logger)
    {
    }

    Logger::Logger(const Char* mod_name, Int log_level)
    {
    }

    Logger::Logger(const String mod_name, Int log_level)
    {
    }
}
